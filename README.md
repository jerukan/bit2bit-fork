# bit2bit-fork

Reminders for myself:
- `example copy 1.ipynb` and `trainthing.py` contain the training code. Run `trainthing.py` to train.
- Inference is in the bottom half of `example copy 1.ipynb`.
